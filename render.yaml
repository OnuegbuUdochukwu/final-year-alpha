# Render Blueprint Configuration
services:
  - type: web
    name: fyp-nlp
    runtime: docker
    rootDir: nlp-service
    dockerfilePath: Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8000
      - fromGroup: fyp-shared-secrets

  - type: web
    name: fyp-graph
    runtime: docker
    rootDir: graph-service
    dockerfilePath: Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8001
      - fromGroup: fyp-shared-secrets

  - type: web
    name: fyp-gateway
    runtime: docker
    rootDir: api-gateway
    dockerfilePath: Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: NLP_SERVICE_URL
        value: http://fyp-nlp:8000
      - key: GRAPH_SERVICE_URL
        value: http://fyp-graph:8001
      - key: ALLOWED_ORIGINS
        value: https://fyp-frontend.onrender.com
      - fromGroup: fyp-shared-secrets

  - type: web
    name: fyp-frontend
    runtime: docker
    rootDir: frontend
    dockerfilePath: Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 80
      - key: VITE_GATEWAY_URL
        value: https://fyp-gateway.onrender.com

envVarGroups:
  - name: fyp-shared-secrets
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NEO4J_URI
        value: bolt://localhost:7687
      - key: NEO4J_USER
        value: neo4j
      - key: NEO4J_PASSWORD
        value: password
      - key: SUPABASE_PG_URL
        value: ""
