github link = [https://github.com/OnuegbuUdochukwu/final-year-alpha.git](https://github.com/OnuegbuUdochukwu/final-year-alpha.git)

upload a resume → see your skills on a radar chart → set a goal → generate a roadmap → view your future resume.

Ready to Run
To bring the full system up:

bash
docker-compose up --build
Then open http://localhost:5173 and walk through:

Login with student / fyp2024
Upload a PDF resume → real NLP parse + skill radar
Set goal (e.g. "Data Scientist") → real A* path loads
Click "✓ Mark Complete" on any step → roadmap auto-recalculates from your new skill position